<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anant.iitbhuvaranasi">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_iitbhu"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ClubPage"></activity>
        <activity android:name=".Full_screen_imageActivity" />
        <activity android:name=".Clubs_group" />
        <activity android:name=".Feedfragment_notifcation_Activity" /> <!-- <activity android:name=".ClubFeed"></activity> -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>